# TEST_SPEC.md — transit-lens

<!-- scaffold template v1.30.0 から展開(2026-09-05) -->

## 実行規約

- Python 側: `.venv/Scripts/python.exe -m pytest -q`。マーカー: `unit` / `integration` / `validation`
- **既定の `pytest -q` は `integration` を外している**(`pytest.ini` の `addopts`)。
  中断と再開の検査は学習を 4 回起動するので、共有機では数分かかる。
  **skip は緑ではない** —— 出荷前に `pytest -q -m integration` を別に走らせること
- TypeScript 側: `npx vitest run`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す

## 期待値の出所(HC-016)

| 出所 | 本プロジェクトでの実物 |
|---|---|
| SPEC の条項 | 前処理の刻み方(§5 P-4/P-5)、分割の規則(§6.2)、頭部の判定規則(§6.1) |
| 外部権威 | ① Shallue & Vanderburg (2018) の TCE 件数(20,367 / 15,737 = PC 3,600・AFP 9,596・NTP 2,541)、② 教科書的な値 —— 地球が太陽を通過するときの深さ約 84 ppm・継続時間約 13 時間 |
| 実測 | 覆う/覆わないのスプライン当てはめの偏り(2026-09-05・4 通り)、二実装照合の最大差 |
| 二実装照合 | Python(`training/tl/`)が権威、TypeScript(`src/lib/`)が従。フィクスチャは base64 の float64 で、**十進に丸めない**(丸めると照合が緩む) |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
唯一の例外は G-01 の TCE 件数で、これは**外部権威の公表値**なので定数で書き、
食い違ったら落とす(台帳が更新されたのなら、更新されたと分かる形で落ちてほしい)。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `data/labels/dr24_tce.csv` | NASA Exoplanet Archive TAP(2026-09-05 取得) | 外部権威。件数は published 値と一致 |
| `tests/fixtures/views.json` | `training/make_fixtures.py`(Python 側が権威) | 二実装照合。4 事例(短周期・標準・長周期・雑音に埋もれた窪み) |
| `tests/fixtures/physics.json` | `training/make_physics_fixture.py`(Python 側が権威) | 二実装照合 + 教科書の値 |
| 合成トランジット(箱形・丸底) | 解析解 | 前処理の刻み・正規化・穴埋めの検査 |

## ケース一覧

**「対応要求」には SPEC の ID を必ず書く**(HC-157)。品質ゲート `G-xx` を確かめるケースには
その `G-xx` を書く。

### Python(`pytest`)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-002 | F-02 | `phase_fold` に周期の整数倍だけずらした時刻を与える | 同じ位相に畳まれる(解析解) |
| T-003 | F-02 | ビンの刻み方(最初のビンが x_min から、最後が x_max で終わる) | y=x を入れるとビン中央値がビン中心に一致 |
| T-003b | G-02 | 点の無いビン | NaN で返り、個数が数えられる |
| T-006 | F-02 | 空ビンの穴埋め | 前後からの線形補間・端は端の値 |
| T-007 | F-02 | 正規化 | 中央値 0・最小値 −1 |
| T-004 | F-02 | global ビュー(箱形トランジット) | 最深ビンの並びの中心が添字 1000 |
| T-005 | F-02 | local ビュー | 窓は ±4 継続時間。深いビンの割合が解析値 1/8 |
| T-005b | F-02 | 周期が短い場合の local ビュー | 窓が ±P/2 で切られ、割合が 1/5 |
| T-008 | G-02 | 空ビンが 25% を超える入力 | `ViewError` で落ちる(黙って埋めない) |
| T-009 | F-02 | 通過を覆う真偽配列 | 覆う割合が 2·duration/period |
| T-010 | F-02 | 欠測での区間分割 | 0.75 日以上の空きで割れる |
| T-011 | F-02 | スプラインが真の低周波変動を回復する | 雑音水準の 3 倍以内 |
| T-012 | F-02 | 深い通過(20σ)は覆っても覆わなくても深さが保たれる | どちらも真値 ±6% |
| T-012b | F-02 | 浅く長い通過は、覆わないと深さを食われる | 覆った方が真値に近い(**覆う実装の陽性対照**) |
| T-013 | G-02 | 当てられない区間 | NaN を返す(黙って値を作らない) |
| T-014 | G-01 | ラベル台帳の件数 | published 値と一致(20,367 / 3,600 / 9,596 / 2,541) |
| T-015 | G-03 | 学習・検証・試験の分割 | 集合間に共通 KIC が 0 件 |
| T-016 | G-02 | 生成された全ビュー | NaN / Inf が 0 個・値域が [−1, ∞) |
| T-017 | G-10 | 日本語本文の字種 | キリル文字の混入 0 件 |
| T-018 | F-06 | 畳み込み枝の出力長 | global 59 点 / local 46 点(maxpool の定義から手で導ける値) |
| T-019 | F-06 | **ロジット = 視線の平均 + バイアス** | 恒等式なので許容は浮動小数の丸めだけ(< 1e-5) |
| T-020 | G-05 | AstroNet 型の頭部の視線 | 0 を返す(出せないものに近似値を作らない) |
| T-021 | G-06 | ONNX 書き出し | PyTorch との最大絶対差 < 1e-5・出力の形が (N,59) / (N,46) |
| T-022 | F-03 | 完走した学習の再開点(`integration`) | 完走後に `<tag>_resume.pt` が残らない。残ると次回**黙って前回の続き**から始まる |
| T-023 | F-03 | 中断した学習の再開(`integration`) | 再開点の epoch の次から始まり、history が引き継がれる(1..5 が揃う) |

### TypeScript(`vitest`)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-101 | G-06 | `views.ts` と Python のビュー生成 | 4 事例すべてで最大絶対差 < 1e-12 |
| T-102 | F-02 | `views.ts` の部品(折りたたみ・ビン・穴埋め・正規化) | 解析解に一致 |
| T-103 | G-06 | **照合の陽性対照** —— local のビン幅をわざと重なりの無い値にする | 1e-6 を超える差が出る(出なければ照合は何も見ていない) |
| T-104 | F-07 | 地球/太陽の通過 | 深さ 83–85 ppm・継続 12.5–13.5 時間(教科書の値) |
| T-105 | G-06 | `physics.ts` と Python の物理 | 定数・導出量・窪みの形・掩蔽の減光率が < 1e-12 |
| T-106 | F-08 | 掩蔽の形 | 小さい相手は平底(平坦度 > 0.99)・大きい相手は V 字(< 0.85) |
| T-107 | G-07 | 配布物 `metrics.json` / `demo.json` / `scores.json` | 画面に出る数がすべて生成物に存在し、ギャラリーは全件 `split=test` |
| T-108 | N-02 / G-11 | ビルド生成物 | ONNX が 5 MB 以下・`out/` にサーバ実行物が無い |

### 実ブラウザ(`scripts/verify-browser.mjs`)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-201 | N-05 / F-06 | 本番と同じ静的書き出しを配って開く | ギャラリー 6 枚・判定ゲージが数字を出す・視線が描かれる |
| T-202 | N-05 / F-07 | つまみを動かす | 判定が変わる。惑星を小さくすると 50% を割る |
| T-203 | N-01 | 読み込み中の通信先 | 自オリジンとフォント以外へ出ない |

## 未実装のゲート

- **G-05 / G-08** は測定スクリプト(`training/measure.py`)が判定する。
  テストからは `metrics.json` に値が入っていることだけを見る(T-107)
- **G-09**(誇張の明記)は文字列検査 + 目視。T-107 で「拡大して表示」「引き伸ばして」の
  文言が画面に存在することを見る
