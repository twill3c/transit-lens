# SPEC.md — transit-lens(Transit Lens 観測卓)

<!-- scaffold template v1.30.0 から展開(2026-09-05)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

NASA Kepler 宇宙望遠鏡が 4 年間測り続けた恒星の明るさから、**1 次元 CNN が惑星の
通過(トランジット)を見つける過程を、専門知識なしで最後まで追える観測卓**にする。

このプロジェクトは二つの顔を持つ。

- **実装訓練として** —— ディープラーニングの実務工程(外部データの採録・前処理・
  不均衡データの学習・評価設計・推論器の配布)を、公開データだけで一周する。
  正解は Shallue & Vanderburg (2018) の AstroNet が置いてある(AUC 0.988)。
  「それらしい結果」で止まらないよう、**同一のラベル集合・同一の前処理定義**を使い、
  published な数値と突き合わせられる形にする
- **公開アプリとして** —— 利用者は KIC 番号を打たない。星を選ぶか、つまみで自分の惑星を
  置くだけで、モデルの判定・視線・答え合わせが順に出る。サーバ側の計算は 0 で、
  推論はブラウザ内の onnxruntime-web が行う(課金経路ゼロ)

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | NASA Exoplanet Archive の `q1_q17_dr24_tce` から TCE 台帳を取得し、`av_training_set` を教師ラベルとする(PC / AFP / NTP、UNK は除外) | must |
| F-02 | MAST の Kepler 長時間ケイデンス光度曲線(PDCSAP)を取得し、AstroNet 準拠の前処理で **global ビュー 2001 点 / local ビュー 201 点**を生成する | must |
| F-03 | 二枝 1D CNN を学習し、AUC・適合率・再現率を**恒星単位で分割した**保留集合で測る | must |
| F-04 | 学習済みモデルを ONNX で書き出し、ブラウザ内(onnxruntime-web)で同一の出力を得る | must |
| F-05 | 画面 入力 1「星を選ぶ」 —— カード 6 枚。ミニ曲線はその星の**実データ**。誤検出の例を混ぜる | must |
| F-06 | 画面 出力 1–3「観測卓」 —— 判定ゲージ / 星と惑星の模式アニメ / 折りたたみ後の曲線に**モデルの視線**を重ねる / NASA アーカイブとの答え合わせ | must |
| F-07 | 画面 入力 2「自分で惑星を置く」 —— 大きさ・周期・ばらつきの 3 つのつまみから光度曲線を合成し、**同じ ONNX モデルに通して**判定を返す | must |
| F-08 | 画面 出力 4「なぜ間違えるのか」 —— 惑星の平底の窪みと食連星の V 字を並べる | should |
| F-09 | 画面 出力 5「見逃しと空振りの取り引き」 —— 保留集合の**実スコア分布**にしきい値を引き、混同行列を出す | must |
| F-10 | 画面 入力 3「曲線を手描き」 —— マウスで窪みを描いて判定を返す | could |
| F-11 | フリート共通フッタ(5 項目・下部固定) | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | サーバ側の実行を持たない(cron 0・API ルート 0・DB 0)。Next.js の静的書き出しのみ | `out/` に `.html`/`.js`/`.onnx` 以外の実行物が無いことを検査 |
| N-02 | 初回読み込みで転送する重量物は ONNX モデル 1 個。**5 MB 以下** | ビルド後に実測 |
| N-03 | 出典表示 —— NASA Exoplanet Archive / MAST / Kepler ミッションを明記する | ページ内検査 |
| N-04 | 日本語本文にキリル文字を混入させない | `harness/text_hygiene.py` |
| N-05 | 実ブラウザで全画面を検品する(検査が緑でも動かない故障を捕まえる) | スクリーンショット |

## 4. データ出典と権利

| 出典 | 内容 | 権利 |
|---|---|---|
| NASA Exoplanet Archive `q1_q17_dr24_tce` | TCE 台帳 20,367 件・教師ラベル `av_training_set` | NASA 制作物。パブリックドメイン(出典表示を行う) |
| MAST(Barbara A. Mikulski Archive for Space Telescopes) | Kepler 長時間ケイデンス光度曲線 FITS | 同上 |

**取得日**: 2026-09-05。ラベル台帳は `data/labels/dr24_tce.csv` にリポジトリ同梱する
(1.4 MB)。光度曲線 FITS は同梱しない(全体で数十 GB)。

## 5. 前処理仕様(AstroNet 準拠)

Shallue & Vanderburg (2018) *AJ* 155:94 の §3 に合わせる。**定義を写して自分で書き直す**
のではなく、同じ手続きを実装し、値が食い違ったら実装側を疑う。

| 段 | 手続き |
|---|---|
| P-1 | 全四半期の PDCSAP_FLUX を連結する。各四半期の中央値で割って規格化する |
| P-2 | 当該 TCE の通過部分(周期・エポック・継続時間から算出)を**覆った上で**、基底スプラインを当てる。スプラインで割って低周波の変動を除く |

**P-2 は参照実装から意図的に外している。** AstroNet は覆わず 3σ クリップだけに任せる。
どちらが良いかを合成データで測った(2026-09-05・真の深さ 4e-4・雑音 3e-4):

| 変動の周期 | 継続時間 | 覆った | 覆わない |
|---|---|---|---|
| 2.5 日 | 0.60 日 | +52% | **−103%(通過が消える)** |
| 8.0 日 | 0.60 日 | +4% | −34% |
| 2.5 日 | 0.15 日 | −21% | −57% |
| 18.0 日 | 0.25 日 | −2% | −11% |

4 通りすべてで覆った方が真値に近い。ただし**覆えば正しいという話ではない** ——
変動の周期が覆う幅に近いと、覆った側も +52% 過大評価する。
深い通過(雑音の 20 倍)では両者に差が出ない(3σ クリップが先に落とすため)。
検査は `tests/test_spline.py` の T-012 / T-012b。
| P-3 | 周期で位相折りたたみする |
| P-4 | **global ビュー** —— 位相全域を 2001 の等幅ビンに分け、各ビンの中央値を取る |
| P-5 | **local ビュー** —— 通過の中心 ±(4 × 継続時間)の窓を 201 ビンに分ける。ビン幅は 0.16 × 継続時間(隣と重なる) |
| P-6 | どちらのビューも、中央値を引いてから最小値の絶対値で割る(中央値 0・最小値 −1) |

**空ビンの扱い** —— 前後の非空ビンから線形補間する。全ビンの 25% 超が空の TCE は捨て、
捨てた件数を記録する。

## 6. モデル仕様

AstroNet の `local_global` 構成を土台にする。ただし**説明可能性のために頭部を変える**
(下記 §6.1)。

| 枝 | 入力 | 構成 | 出力長 |
|---|---|---|---|
| global | 2001 × 1 | conv ブロック 5 段(フィルタ 16/32/64/128/256 × 幅、カーネル 5、各段 conv×2 → maxpool 5 stride 2) | 59 |
| local | 201 × 1 | conv ブロック 2 段(フィルタ 16/32 × 幅、カーネル 5、各段 conv×2 → maxpool 7 stride 2) | 46 |

**幅は 0.5 で出荷する(published は 1.0)。** 計算資源の都合であり、精度のための選択ではない。
実測(2026-09-05・i7-1185G7 物理 4 コア・4 スレッド・学習集合 12,516 件):

| 幅 | パラメータ | 1 epoch |
|---|---|---|
| 1.0 | 664,577 | 267 秒 |
| **0.5** | **166,529** | **115 秒** |

3 本(出荷・比較・陰性対照)を同条件で回す必要があるため、幅 1.0 では 6 時間を超える。
**published の AUC 0.988 との差は、(a) 幅を半分にしたこと、(b) 学習ステップ数が
参照実装の 1/5 程度であること、(c) 分割を恒星単位にしたこと の三つを含む** ——
どれか一つに帰せない。§9 の実測値はその前提で読む。

### 6.1 頭部の二択(測って決める)

| 案 | 頭部 | 視線の出し方 | 性質 |
|---|---|---|---|
| **H-A(CAM 型)** | 各枝を大域平均プーリング → 連結(288) → 全結合 1 層 | **厳密**。ロジットは各位置の寄与の平均に厳密に分解でき、前向き計算だけで出る | 表現力を捨てる |
| H-B(AstroNet 型) | 平坦化 → 全結合 512 × 4 → シグモイド | 近似。ブラウザでは遮蔽感度(前向き N 回)に頼る | published 構成に一致 |

**判定規則(G-05)**: 両方を同一の分割・同一の種で学習し、保留集合の AUC を測る。
`AUC(H-B) − AUC(H-A) ≤ 0.010` なら H-A を出荷する(視線が厳密になる利得を取る)。
超えたら H-B を出荷し、視線は遮蔽感度に切り替え、**画面にもその旨を書く**。
どちらを出したかは実測値とともに SPEC に残す。

### 6.2 分割

**恒星単位で分割する**(同一 KIC の TCE が学習集合と保留集合にまたがらないようにする)。
AstroNet 論文は TCE 単位の無作為分割だが、同一星の複数 TCE は光度曲線を共有するので
漏れが起きる。**この違いは AUC を下げる方向に働く見込みであり、published 値との差は
この分だけ割り引いて読む**(見込み。実測は §9 に置く)。

割合は 80 / 10 / 10(学習 / 検証 / 試験)。種は 20260905 に固定する。

## 7. 目玉(事前登録)

**測る前に書く。落ちたら落ちたと書いて残す。**

- **目玉 1(検出限界)** —— 合成トランジットの注入回収で、モデルの検出確率が 50% を
  横切る点を求める。これが Kepler ミッション自身の検出しきい値 **7.1σ(MES)** の
  ±2σ 以内に入るか。**予測: 入る。** 根拠は、モデルが学習した TCE 集合自体が 7.1σ で
  切られているため、その境界を写し取るはずだという推論
- **目玉 2(視線)** —— 視線(CAM または遮蔽感度)の質量のうち、通過窓(中心 ±1 継続時間)に
  落ちる割合を local ビューで測る。**予測: 正例に対して 0.5 を超え、位相を無作為化した
  対照より有意に高い**(窓は全幅 8 継続時間のうち 2 なので、無情報なら 0.25)

### 7.1 結果(2026-09-06 実測)—— **予測は二つとも外れた**

**目玉 1: 不成立。** 5 通りの設定で 50% 交差点は **9.61 / 9.70 / 9.79 / 11.48 / 11.52 σ**
(中央値 9.79σ)。予測した帯 7.1 ± 2 = [5.1, 9.1] の**外側**にすべて落ちた。
向きも一貫していて、モデルは**ミッション自身のしきい値より 2.5〜4.4σ 厳しい**。

外れた理由の見立て(これは実測ではなく解釈である): 7.1σ は
**候補として拾う**線であって**惑星である**線ではない。7.1σ ぎりぎりで拾われた TCE の
多くは AFP / NTP なので、「惑星である確率 50%」の線が 7.1σ より上に来るのは
むしろ筋が通っている。**予測を立てたときに、この二つの線を同じものとして扱っていた。**

**目玉 2: 登録した物差しでは不成立。しかも向きが逆だった。**
正の寄与の質量が通過窓に落ちる割合は **0.047**(中央値 0.000)で、
巡回ずらしの対照 **0.256**・無情報の基準 0.254 を**下回った**。
対照を上回ったのは 335 件中 **6.6%** だけ。

理由は測って分かった —— **local 枝は「惑星である証拠」ではなく「この窪みは怪しい」という
減点として働いている。** ロジットへの寄与の平均は、惑星と判定した正例で **−1.26**、
誤検出と判定した負例で **−8.01**。判別を担っているのは符号ではなく**大きさ**である。
登録した物差しは正の側しか見ていなかったので、この働き方を測れない。

**事後の物差し(結果を見てから足した)**: 中央値からの隔たり |cam − median| の質量で
測り直すと、通過窓に落ちる割合は **0.760**(中央値 0.893)、対照 0.265、
対照を上回った割合 **91.3%**。**ただしこれは結果を見てから選んだ物差しなので、
目玉 2 の合格の根拠にはしない。** 判定は「不成立」のまま残し、両方を画面に出す。

この実測は画面を一つ作り直させた —— 視線の帯は正の寄与だけを描いていたので、
**モデルの働き方が見えない絵**になっていた。中央値からの隔たりを符号つきで描き、
上向き(藍)= 惑星の側 / 下向き(朱)= 惑星でない側 に変更した。

## 8. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | ラベル台帳の件数が published 値と一致する —— 全 TCE 20,367 件、教師ラベル付き 15,737 件(PC 3,600 / AFP 9,596 / NTP 2,541) | 一致しなければ落とす |
| G-02 | 前処理の欠測規則が働いている —— 捨てた TCE の件数と理由が記録され、生成された各ビューに NaN / Inf が 0 個 | 違反 1 件で落とす |
| G-03 | **循環の禁止** —— 学習・検証・試験の分割が恒星単位で、集合間に共通 KIC が 0 件 | 1 件でも共有したら落とす |
| G-04 | **陰性対照** —— ラベルを無作為に入れ替えて学習したモデルの**クラス分離**(正例と負例の出力平均の差 ÷ 出力の標準偏差)が、本モデルの 1/5 未満 | 超えたら学習経路に漏れがある |

> **G-04 の物差しは二度書き直した(2026-09-06)。経緯を残す。**
>
> **一度目の誤り: 帰無を 0.5 と決めつけていた。** 当初は「AUC が 0.55 未満」。
> 同じ構造の**未学習**モデルの試験 AUC を 5 通りの初期値で測ると
> **0.3512 / 0.4486 / 0.4973 / 0.5721 / 0.7245**(平均 0.519)に散った ——
> **乱数の初期値だけで AUC 0.72 が出る。**
>
> **二度目の誤り: 退化した予測器に AUC を当てていた。** 実測すると、
> ラベルを入れ替えて学習したモデルの出力は**ほぼ定数**になった ——
> 平均 **0.2197**(学習集合の正例率 0.2256 とほぼ同じ)、標準偏差 **0.00177**
> (本モデルは 0.33986)。正例の平均 0.2199 / 負例の平均 0.2196 で、**差は 0.0003**。
> ところがその微小な揺らぎが MES と順位相関 **+0.60** を持ち、DR24 では MES の高い側を
> 深い食連星が占めるため、**AUC は 0.117 まで下がる**(検証 AUC は 22 epoch かけて
> 0.597 → 0.117 へ単調に低下した)。**ラベルを一切学んでいないのに AUC は 0.5 から
> 大きく離れる。** AUC は順位だけを見るので、定数予測器に対して意味を持たない。
>
> **今の物差し**: クラス分離(SD 単位)。定数予測器では素直に 0 に近づく。
> 実測は **陰性対照 0.134 SD / 本モデル 1.905 SD**(14 倍)。漏れは無い。
>
> これは [[HC-181]] とまったく同じ穴である —— **物差しを空回しせずに閾値を書いた。**
> 同じループで三度踏んだので、失敗の記録としてここに残す。
| G-05 | 頭部の選定を §6.1 の規則で行い、両案の AUC を実測して SPEC に残した | 未実測で出荷しない |

> **G-05 の結果(2026-09-06 実測・両案とも 22 epoch の同条件)**:
> AstroNet 型 **0.9682** / CAM 型 **0.9679**、差 **+0.0003**。
> §6.1 の規則(差 ≤ 0.010 なら CAM 型)どおり **CAM 型を出荷する**。
> **視線が厳密になることの代償は、AUC で 0.0003 だった。**
> ただし代償が無いわけではない —— AstroNet 型は epoch 4 で検証 AUC 0.9659 に達し
> (CAM 型が 22 epoch かけて到達する水準)、最良は epoch 17。
> **収束の速さでは AstroNet 型がはっきり優る。到達点はほぼ同じ。**
> パラメータは 5,198,833 対 166,529 で 31 倍の差がある。
>
> なお、この 2 つのゲートは一度「共有機の占有待ちで未達」の状態で出荷した。
> 実測で当該プロセスが取れたのは 0.7 コアで、22 epoch に 5 時間かかる見込みだった。
> そのあいだ画面は **「未測定」と表示**していた(0.000 とは書かない —— 
> 測っていないことと測って 0 だったことは違う)。占有が空いてから測って差し替えた。
| G-06 | **二実装照合** —— PyTorch と onnxruntime(web/node)の出力が、試験集合全件で最大絶対差 1e-5 未満 | 超えたら落とす |
| G-07 | 出荷する試験集合の成績(AUC・適合率・再現率)が、画面に出す数値と同一の値から作られている(画面の数字を手で書かない) | 生成物の突合 |
| G-08 | 目玉 1・目玉 2 を実測し、成否にかかわらず SPEC と画面に結果を残した | 未実測で出荷しない |
| G-09 | 画面に出る「拡大して表示」等の誇張が、その場に明記されている | 目視 + 文字列検査 |
| G-10 | 日本語本文にキリル文字が混入していない | `text_hygiene` |
| G-11 | 静的書き出しのみ(サーバ実行 0) | ビルド生成物の検査 |

## 9. 実測値(測った後にここへ置く)

| 項目 | 値 | 実測日 |
|---|---|---|
| 取得できた光度曲線の星数 / 目標 | **9,865 / 9,865**(公開ツリーに無い星は 0 件だった) | 2026-09-05 |
| 前処理を通った TCE 数 / 15,737 | **15,636(99.36%)**。捨てた 101 件はすべて「窓に点が足りない」か「空ビンが 25% 超」 | 2026-09-05 |
| 分割(恒星単位・種 20260905) | 学習 12,516 / 検証 1,506 / 試験 1,614(星 9,799 個・共有 KIC 0 件) | 2026-09-05 |
| 試験 AUC(H-A = 出荷) | **0.9679**(AP 0.8821 / 適合率 0.8568 / 再現率 0.8211・保留 1,614 件のうち正例 408 件) | 2026-09-06 |
| 試験 AUC(H-B = AstroNet 型) | **0.9682**(AP 0.8887 / 適合率 0.8416 / 再現率 0.8333・最良 epoch 17・パラメータ 5,198,833)。**差 +0.0003 → CAM 型を出荷** | 2026-09-06 |
| 未学習の基準線(同じ初期値) | **0.4677**。初期値を 5 通り振ると 0.3512〜0.7245(平均 0.519) | 2026-09-06 |
| 陰性対照(クラス分離) | **0.134 SD**(正例 0.2199 / 負例 0.2196 / 出力の標準偏差 0.00177)。本モデルは **1.905 SD**(0.7071 / 0.0596 / 0.33986)。**14 倍の差 —— 漏れは無い** | 2026-09-06 |
| 陰性対照(AUC・**物差しとして不適**) | 最良 checkpoint で 0.5621、22 epoch 後は 0.117。検証 AUC は 0.597 → 0.117 へ単調低下。**ほぼ定数の出力を順位で測った産物**で、漏れの証拠ではない | 2026-09-06 |
| 二実装照合の最大絶対差 | **2.980e-07**(保留集合 1,614 件全件・基準 1e-5) | 2026-09-06 |
| モデルの大きさ | パラメータ 166,529 / ONNX 677,451 バイト(基準 5 MB) | 2026-09-06 |
| 目玉 1 の 50% 交差点 | **9.61 / 9.70 / 9.79 / 11.48 / 11.52 σ**(中央値 9.79)—— **予測の帯 [5.1, 9.1] の外。不成立** | 2026-09-06 |
| 目玉 2 の通過窓内比率(登録した物差し) | **0.047**(対照 0.256・無情報 0.254・対照超 6.6%)—— **不成立、しかも逆向き** | 2026-09-06 |
| 目玉 2(事後の物差し・参考) | 0.760(中央値 0.893・対照 0.265・対照超 91.3%)。**結果を見てから足した物差しなので合格の根拠にしない** | 2026-09-06 |
| 枝ごとの寄与(ロジット) | 惑星と判定した正例: global **+2.93** / local **−1.26**。誤検出と判定した負例: global **−2.24** / local **−8.01** | 2026-09-06 |

## 10. スコープ外

- **新しい惑星の発見を主張しない。** 本アプリは既知の TCE に対する再現であり、
  未検証の候補に判定を下す用途を持たない
- TESS・K2 は扱わない(Kepler DR24 に限る)
- 短時間ケイデンス(1 分)は扱わない
- 恒星パラメータの推定、軌道要素の当てはめは行わない
- 学習をブラウザ内で行わない(推論のみ)
