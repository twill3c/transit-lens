# SPEC.md — transit-lens(Transit Lens 観測卓)

<!-- scaffold template v1.30.0 から展開(2026-09-05)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

NASA Kepler 宇宙望遠鏡が 4 年間測り続けた恒星の明るさから、**1 次元 CNN が惑星の
通過(トランジット)を見つける過程を、専門知識なしで最後まで追える観測卓**にする。

このプロジェクトは二つの顔を持つ。

- **実装訓練として** —— ディープラーニングの実務工程(外部データの採録・前処理・
  不均衡データの学習・評価設計・推論器の配布)を、公開データだけで一周する。
  正解は Shallue & Vanderburg (2018) の AstroNet が置いてある(AUC 0.988)。
  「それらしい結果」で止まらないよう、**同一のラベル集合・同一の前処理定義**を使い、
  published な数値と突き合わせられる形にする
- **公開アプリとして** —— 利用者は KIC 番号を打たない。星を選ぶか、つまみで自分の惑星を
  置くだけで、モデルの判定・視線・答え合わせが順に出る。サーバ側の計算は 0 で、
  推論はブラウザ内の onnxruntime-web が行う(課金経路ゼロ)

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | NASA Exoplanet Archive の `q1_q17_dr24_tce` から TCE 台帳を取得し、`av_training_set` を教師ラベルとする(PC / AFP / NTP、UNK は除外) | must |
| F-02 | MAST の Kepler 長時間ケイデンス光度曲線(PDCSAP)を取得し、AstroNet 準拠の前処理で **global ビュー 2001 点 / local ビュー 201 点**を生成する | must |
| F-03 | 二枝 1D CNN を学習し、AUC・適合率・再現率を**恒星単位で分割した**保留集合で測る | must |
| F-04 | 学習済みモデルを ONNX で書き出し、ブラウザ内(onnxruntime-web)で同一の出力を得る | must |
| F-05 | 画面 入力 1「星を選ぶ」 —— カード 6 枚。ミニ曲線はその星の**実データ**。誤検出の例を混ぜる | must |
| F-06 | 画面 出力 1–3「観測卓」 —— 判定ゲージ / 星と惑星の模式アニメ / 折りたたみ後の曲線に**モデルの視線**を重ねる / NASA アーカイブとの答え合わせ | must |
| F-07 | 画面 入力 2「自分で惑星を置く」 —— 大きさ・周期・ばらつきの 3 つのつまみから光度曲線を合成し、**同じ ONNX モデルに通して**判定を返す | must |
| F-08 | 画面 出力 4「なぜ間違えるのか」 —— 惑星の平底の窪みと食連星の V 字を並べる | should |
| F-09 | 画面 出力 5「見逃しと空振りの取り引き」 —— 保留集合の**実スコア分布**にしきい値を引き、混同行列を出す | must |
| F-10 | 画面 入力 3「曲線を手描き」 —— マウスで窪みを描いて判定を返す | could |
| F-11 | フリート共通フッタ(5 項目・下部固定) | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | サーバ側の実行を持たない(cron 0・API ルート 0・DB 0)。Next.js の静的書き出しのみ | `out/` に `.html`/`.js`/`.onnx` 以外の実行物が無いことを検査 |
| N-02 | 初回読み込みで転送する重量物は ONNX モデル 1 個。**5 MB 以下** | ビルド後に実測 |
| N-03 | 出典表示 —— NASA Exoplanet Archive / MAST / Kepler ミッションを明記する | ページ内検査 |
| N-04 | 日本語本文にキリル文字を混入させない | `harness/text_hygiene.py` |
| N-05 | 実ブラウザで全画面を検品する(検査が緑でも動かない故障を捕まえる) | スクリーンショット |

## 4. データ出典と権利

| 出典 | 内容 | 権利 |
|---|---|---|
| NASA Exoplanet Archive `q1_q17_dr24_tce` | TCE 台帳 20,367 件・教師ラベル `av_training_set` | NASA 制作物。パブリックドメイン(出典表示を行う) |
| MAST(Barbara A. Mikulski Archive for Space Telescopes) | Kepler 長時間ケイデンス光度曲線 FITS | 同上 |

**取得日**: 2026-09-05。ラベル台帳は `data/labels/dr24_tce.csv` にリポジトリ同梱する
(1.4 MB)。光度曲線 FITS は同梱しない(全体で数十 GB)。

## 5. 前処理仕様(AstroNet 準拠)

Shallue & Vanderburg (2018) *AJ* 155:94 の §3 に合わせる。**定義を写して自分で書き直す**
のではなく、同じ手続きを実装し、値が食い違ったら実装側を疑う。

| 段 | 手続き |
|---|---|
| P-1 | 全四半期の PDCSAP_FLUX を連結する。各四半期の中央値で割って規格化する |
| P-2 | 当該 TCE の通過部分(周期・エポック・継続時間から算出)を**覆った上で**、基底スプラインを当てる。スプラインで割って低周波の変動を除く |
| P-3 | 周期で位相折りたたみする |
| P-4 | **global ビュー** —— 位相全域を 2001 の等幅ビンに分け、各ビンの中央値を取る |
| P-5 | **local ビュー** —— 通過の中心 ±(4 × 継続時間)の窓を 201 ビンに分ける。ビン幅は 0.16 × 継続時間(隣と重なる) |
| P-6 | どちらのビューも、中央値を引いてから最小値の絶対値で割る(中央値 0・最小値 −1) |

**空ビンの扱い** —— 前後の非空ビンから線形補間する。全ビンの 25% 超が空の TCE は捨て、
捨てた件数を記録する。

## 6. モデル仕様

AstroNet の `local_global` 構成を土台にする。ただし**説明可能性のために頭部を変える**
(下記 §6.1)。

| 枝 | 入力 | 構成 |
|---|---|---|
| global | 2001 × 1 | conv ブロック 5 段(フィルタ 16/32/64/128/256、カーネル 5、各段 conv×2 → maxpool 5 stride 2) |
| local | 201 × 1 | conv ブロック 2 段(フィルタ 16/32、カーネル 5、各段 conv×2 → maxpool 7 stride 2) |

### 6.1 頭部の二択(測って決める)

| 案 | 頭部 | 視線の出し方 | 性質 |
|---|---|---|---|
| **H-A(CAM 型)** | 各枝を大域平均プーリング → 連結(288) → 全結合 1 層 | **厳密**。ロジットは各位置の寄与の平均に厳密に分解でき、前向き計算だけで出る | 表現力を捨てる |
| H-B(AstroNet 型) | 平坦化 → 全結合 512 × 4 → シグモイド | 近似。ブラウザでは遮蔽感度(前向き N 回)に頼る | published 構成に一致 |

**判定規則(G-05)**: 両方を同一の分割・同一の種で学習し、保留集合の AUC を測る。
`AUC(H-B) − AUC(H-A) ≤ 0.010` なら H-A を出荷する(視線が厳密になる利得を取る)。
超えたら H-B を出荷し、視線は遮蔽感度に切り替え、**画面にもその旨を書く**。
どちらを出したかは実測値とともに SPEC に残す。

### 6.2 分割

**恒星単位で分割する**(同一 KIC の TCE が学習集合と保留集合にまたがらないようにする)。
AstroNet 論文は TCE 単位の無作為分割だが、同一星の複数 TCE は光度曲線を共有するので
漏れが起きる。**この違いは AUC を下げる方向に働く見込みであり、published 値との差は
この分だけ割り引いて読む**(見込み。実測は §9 に置く)。

割合は 80 / 10 / 10(学習 / 検証 / 試験)。種は 20260905 に固定する。

## 7. 目玉(事前登録)

**測る前に書く。落ちたら落ちたと書いて残す。**

- **目玉 1(検出限界)** —— 合成トランジットの注入回収で、モデルの検出確率が 50% を
  横切る点を求める。これが Kepler ミッション自身の検出しきい値 **7.1σ(MES)** の
  ±2σ 以内に入るか。**予測: 入る。** 根拠は、モデルが学習した TCE 集合自体が 7.1σ で
  切られているため、その境界を写し取るはずだという推論
- **目玉 2(視線)** —— 視線(CAM または遮蔽感度)の質量のうち、通過窓(中心 ±1 継続時間)に
  落ちる割合を local ビューで測る。**予測: 正例に対して 0.5 を超え、位相を無作為化した
  対照より有意に高い**(窓は全幅 8 継続時間のうち 2 なので、無情報なら 0.25)

## 8. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | ラベル台帳の件数が published 値と一致する —— 全 TCE 20,367 件、教師ラベル付き 15,737 件(PC 3,600 / AFP 9,596 / NTP 2,541) | 一致しなければ落とす |
| G-02 | 前処理の欠測規則が働いている —— 捨てた TCE の件数と理由が記録され、生成された各ビューに NaN / Inf が 0 個 | 違反 1 件で落とす |
| G-03 | **循環の禁止** —— 学習・検証・試験の分割が恒星単位で、集合間に共通 KIC が 0 件 | 1 件でも共有したら落とす |
| G-04 | **陰性対照** —— ラベルを無作為に入れ替えて学習したモデルの試験 AUC が 0.55 未満 | 超えたら学習経路に漏れがある |
| G-05 | 頭部の選定を §6.1 の規則で行い、両案の AUC を実測して SPEC に残した | 未実測で出荷しない |
| G-06 | **二実装照合** —— PyTorch と onnxruntime(web/node)の出力が、試験集合全件で最大絶対差 1e-5 未満 | 超えたら落とす |
| G-07 | 出荷する試験集合の成績(AUC・適合率・再現率)が、画面に出す数値と同一の値から作られている(画面の数字を手で書かない) | 生成物の突合 |
| G-08 | 目玉 1・目玉 2 を実測し、成否にかかわらず SPEC と画面に結果を残した | 未実測で出荷しない |
| G-09 | 画面に出る「拡大して表示」等の誇張が、その場に明記されている | 目視 + 文字列検査 |
| G-10 | 日本語本文にキリル文字が混入していない | `text_hygiene` |
| G-11 | 静的書き出しのみ(サーバ実行 0) | ビルド生成物の検査 |

## 9. 実測値(測った後にここへ置く)

| 項目 | 値 | 実測日 |
|---|---|---|
| 取得できた光度曲線の星数 / 目標 | 実測後に置き直す | — |
| 前処理を通った TCE 数 / 15,737 | 実測後に置き直す | — |
| 試験 AUC(H-A / H-B) | 実測後に置き直す | — |
| 陰性対照の AUC | 実測後に置き直す | — |
| 二実装照合の最大絶対差 | 実測後に置き直す | — |
| 目玉 1 の 50% 交差点 | 実測後に置き直す | — |
| 目玉 2 の通過窓内比率(正例 / 対照) | 実測後に置き直す | — |

## 10. スコープ外

- **新しい惑星の発見を主張しない。** 本アプリは既知の TCE に対する再現であり、
  未検証の候補に判定を下す用途を持たない
- TESS・K2 は扱わない(Kepler DR24 に限る)
- 短時間ケイデンス(1 分)は扱わない
- 恒星パラメータの推定、軌道要素の当てはめは行わない
- 学習をブラウザ内で行わない(推論のみ)
